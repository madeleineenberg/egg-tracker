specVersion: "v1"
serviceId: "nyponbacken"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nyponbacken-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
